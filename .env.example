# Security: Secret key for Claude analyze endpoint (optional but recommended for production)
# Generate a strong random key: openssl rand -hex 32
# If not set, endpoint only accepts requests from localhost
CLAUDE_SECRET_KEY=your-secret-key-here

# Optional: Milvus configuration (defaults work for docker-compose setup)
# MILVUS_HOST=milvus-standalone
# MILVUS_PORT=19530
